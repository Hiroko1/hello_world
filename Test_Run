CREATE TABLE Test_Run (
Student_id BIGINT NOT NULL PRIMARY KEY,
Name VARCHAR(50),
Address VARCHAR(50),
GPA SMALLINT,
Course_Load BIGINT);
create unique index Student_id on Test_Run (Student_id);
create index Course_Load on Test_Run (Course_Load);
